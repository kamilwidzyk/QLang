place my_place {
    num my_num = 3;
    num my_arr[?] = [42, 2.31, 32];
    text my_text = "test";
    state my_state;
    
    debug(@my_num);
    debug(@my_arr);
    debug(@my_text);
    debug(2+2*2);
    debug(@my_state);
}

