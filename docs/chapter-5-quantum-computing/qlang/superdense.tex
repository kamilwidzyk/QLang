% Auto-generated by docs/tools/highlight.py - do not edit
\begin{codebox}
place Alice{
    state qA, qB;
    H qA;
    CNOT qA -> qB;
    send qB to Bob as "qubitB";

    obs bits[2];
    print("Enter number 0-3 (two bits): ");
    input(bits, 0..3);
    if (bits[0]) Z qA;
    if (bits[1]) X qA;
    send qA to Bob as "qubitA";
}

place Bob {
    state qB = receive state from Alice named "qubitB";
    state qA = receive state from Alice named "qubitA";
    CNOT qA -> qB;
    H qA;
    obs result[?] = measure [qA, qB];
    println("Decoded bits: %d %d" % [result[0], result[1]]);
}
\end{codebox}
