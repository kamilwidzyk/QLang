function abs(num x) {
    if(x < 0) {
        return 0 - x;
    }
    return x;
}

function minimum(num a, num b) {
    if(a < b) {
        return a;
    }
    return b;
}

function maximum(num a, num b) {
    if(a > b) {
        return a;
    }
    return b;
}
